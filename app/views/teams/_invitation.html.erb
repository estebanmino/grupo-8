<%= form_for Invitation.new , :url => '/invitations/create' do |f| %>
    <%= f.hidden_field :team_id, :value => @team.id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send' %>
<% end %>
