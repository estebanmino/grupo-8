<%= form_for(match) do |f| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :home_team_id %>
    <%= f.number_field :home_team_id, required: true  %>
  </div>

  <div class="field">
    <%= f.label :visit_team_id %>
    <%= f.number_field :visit_team_id, required: true  %>
  </div>

  <div class="field">
    <%= f.label :tournament_id %>
    <%= f.number_field :tournament_id, required: true  %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :time %>
    <%= f.time_select :time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
